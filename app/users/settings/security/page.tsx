import Security from "@/components/dashboard/user/settings/Security";
import React from "react";

const page = () => {
    return (
        <div>
            <Security />
        </div>
    );
};

export default page;
